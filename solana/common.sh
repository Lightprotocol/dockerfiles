#!/bin/bash

SOLANA_CONFIG_DIR="${HOME}/.config/solana"
LEDGER_DIR="${SOLANA_CONFIG_DIR}/bootstrap-validator"

IDENTITY_KEYPAIR="${LEDGER_DIR}/identity.json"
VOTE_KEYPAIR="${LEDGER_DIR}/vote-account.json"
